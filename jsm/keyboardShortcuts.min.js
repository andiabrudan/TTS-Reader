function nc(a) {
  a.shiftKey &&
    a.altKey &&
    40 == a.keyCode &&
    chrome.runtime.sendMessage({ message_id: "backstartorpause", tab_id: T });
}
function Ua() {
  chrome.runtime.sendMessage({ message_id: "backtogglereadability" });
}
var T = 0;
document.addEventListener("keydown", nc, !1);
